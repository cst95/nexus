version: '3.0'

services:
  # mongo:
  #   image: mongo
  #   restart: always
  #   environment:
  #     MONGO_INITDB_ROOT_USERNAME: root
  #     MONGO_INITDB_ROOT_PASSWORD: example
  #   ports:
  #     - 27017:27017
  # mongo-express:
  #     image: mongo-express
  #     restart: always
  #     ports:
  #       - 8081:8081
  #     environment:
  #       ME_CONFIG_MONGODB_ADMINUSERNAME: root
  #       ME_CONFIG_MONGODB_ADMINPASSWORD: example
  #     depends_on:
  #       - mongo
  summoner.api:
    image: nexus-summoner.api:latest
    build:
      context: ./SummonerAPI
    ports:
      - "80:80"
    environment:
      ASPNETCORE_URLS: http://*:80
      ASPNETCORE_ENVIRONMENT: Development
      RIOT_APIKEY: helloworld